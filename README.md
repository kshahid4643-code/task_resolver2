# Task Dependency Priority Resolver

This project resolves task execution order based on dependencies using Python.

## How it works

- Tasks can depend on other tasks
- The resolver calculates the correct execution order
- Detects circular dependencies

## Run the project

```bash
python main.py
```
